---
import { Schema } from "astro-seo-schema";
const { name, image, url } = Astro.props;
---

<Schema
    item={{
        "@context": "https://schema.org",
        "@type": "Person",
        name,
        image,
        url,
    }}
/>
